@model IEnumerable<LMSMVC.Models.Tests>
@{
    ViewBag.Title = "PythonTest";
    Layout = "~/Views/Shared/_LearnerLayout.cshtml";
    <link href="~/Content/Site.css" rel="stylesheet" />
}

<h2>PythonTest</h2>

<meta charset="utf-8" />
<title>Python Programming Test</title>
<br />
<h4><b> Python Programming Test </b></h4>
<br />
     1. Which of the function among will return 4 on the set s = {3, 4, 1, 2}?
<br />
<br />
<select id="item1" name="Item 1">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="Four(s)">Four(s)</OPTION>
    <OPTION VALUE="Sum(s)">Sum(s)</OPTION>
    <OPTION VALUE="Len(s)">Len(s)</OPTION>
</select>

    <! -- Ansr1, Len(s) -->
<br />
<br />
    2. Which operator is right-associative
<br />
<br />
<select id="item2" name="Item 2">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="=">=</OPTION>
    <OPTION VALUE="+">+</OPTION>
    <OPTION VALUE="%">%</OPTION>
</select>
    <! -- Ansr2, = -->
<br />
<br />
    3. Select the correct function among them which can be used to write the data to perform for a binary output?
<br />
<br />
<select id="item3" name="Item 3">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="Write">Write</OPTION>
    <OPTION VALUE="Dump">Dump</OPTION>
    <OPTION VALUE="Binary.output">Binary.output</OPTION>
</select>
<! -- Ansr3, Dump -->
<br />
<br />

    4. Which module is used in python to create Graphics?
<br />
<br />

<select id="item4" name="Item 4">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="Turtle">Turtle</OPTION>
    <OPTION VALUE="Canvas">Canvas</OPTION>
    <OPTION VALUE="Tkinter">Tkinter</OPTION>
</select>
    <! -- Ansr4, Turtle -->
<br />
<br />
<br />
<button onclick="message()">Check your score here</button>
<br />

@using (Html.BeginForm("EditScore", "Tests", new { id = ViewData["id"] }, FormMethod.Post))
{
    <p>Please, enter your score in the below textbox after taking test!</p>
    <br />
    <input type="text" name="score" />
    <input type="Submit" />
    <br />
    <b>@Html.ActionLink("Submit", "SubmitPython", new { })</b>}

<SCRIPT>
    function message() {
        count = 0
        var a1 = document.getElementById('item1');
        var a2 = document.getElementById('item2');
        var a3 = document.getElementById('item3');
        var a4 = document.getElementById('item4');
        if (a1.value == "null" || a2.value == "null" || a3.value == "null" || a4.value == "null") {
            alert('Please answer all the questions')
        }
        else {


            if (a1.value == "Len(s)") {
                count = count + 1
            }
            if (a2.value == "=") {
                count = count + 1
            }
            if (a3.value == "Dump") {
                count = count + 1
            }
            if (a4.value == "Turtle") {
                count = count + 1
            }
            alert('Your Score is ' + (count / 4 * 100) + '%')
        }
        var percentage = (count / 4) * 100;
        return percentage;
    }</SCRIPT>