@model IEnumerable<LMSMVC.Models.Tests>

@{ ViewBag.Title = "Test-C#";
                Layout = "~/Views/Shared/_LearnerLayout.cshtml";
    <link href="~/Content/Site.css" rel="stylesheet" />}

<h2>Take the Test</h2>

<meta charset="utf-8" />
<title>C# Programming Test</title>
<br />
<h4><b> C# Programming Test </b></h4>
<br />
    1. Which of the following converts a type to a 64-bit integer in C#?
<br />
<br />
<select id="item1" name="Item 1">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="ToInt32">ToInt32</OPTION>
    <OPTION VALUE="ToInt64">ToInt64</OPTION>
    <OPTION VALUE="ToSingle">ToSingle</OPTION>
</select>
    <! -- Ansr1, ToInt64 -->
<br />
<br />
    2. Which of the following operator creates a pointer to a variable in C#?
<br />
<br />
<select id="item2" name="Item 2">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="sizeof">sizeof</OPTION>
    <OPTION VALUE="typeof">typeof</OPTION>
    <OPTION VALUE="*">*</OPTION>
</select>
    <! -- Ansr2, * -->
<br />
<br />
    3. C# does not support multiple inheritance. (True or False)
<br />
<br />
<select id="item3" name="Item 3">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="True">True</OPTION>
    <OPTION VALUE="False">False</OPTION>
</select>
    <! -- Ansr3, True -->
<br />
<br />

    4. Which of the following preprocessor directive defines a sequence of characters as symbol in C#?
<br />
<br />

<select id="item4" name="Item 4">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="define">define</OPTION>
    <OPTION VALUE="endregion">endregion</OPTION>
    <OPTION VALUE="undef">undef</OPTION>
</select>
    <! -- Ansr4, define -->
<br />
<br />
<br />
<button onclick="message()">Check your score here</button>
<br />

@using (Html.BeginForm("EditScore", "Tests", new { id = ViewData["id"] }, FormMethod.Post))
{
<p>Please, enter your score in the below textbox after taking test!</p>
                <br />
                                <input type="text" name="score" />
                                                <input type="Submit" />
                                                                <br />
                                                                                <b>@Html.ActionLink("Submit", "SubmitCSharp", new { })</b>}

<SCRIPT>function message() {
        count = 0
        var a1 = document.getElementById('item1');
        var a2 = document.getElementById('item2');
        var a3 = document.getElementById('item3');
        var a4 = document.getElementById('item4');
        if (a1.value == "null" || a2.value == "null" || a3.value == "null" || a4.value == "null") {
            alert('Please answer all the questions')
        }
        else {


            if (a1.value == "ToInt64") {
                count = count + 1
            }
            if (a2.value == "*") {
                count = count + 1
            }
            if (a3.value == "True") {
                count = count + 1
            }
            if (a4.value == "define") {
                count = count + 1
            }
            alert('Your Score is ' + (count / 4 * 100) + '%')
        }
        var percentage = (count / 4) * 100;
        return percentage;
    }</SCRIPT>