@model IEnumerable<LMSMVC.Models.Tests>
@{
    ViewBag.Title = "SQLTest";
    Layout = "~/Views/Shared/_LearnerLayout.cshtml";
    <link href="~/Content/Site.css" rel="stylesheet" />
}

<h2>SQLTest</h2>
<meta charset="utf-8" />
<title>SQL Programming Test</title>
<br />
<h4><b> SQL Programming Test </b></h4>
<br />
    1. Which of the following is not a valid SQL type?
<br />
<br />
<select id="item1" name="Item 1">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="CHARACTER">CHARACTER</OPTION>
    <OPTION VALUE="NUMERIC">NUMERIC</OPTION>
    <OPTION VALUE="DECIMAL">DECIMAL</OPTION>
</select>
    <! -- Ansr1, DECIMAL -->
<br />
<br />
     2. SQL permits attribute names to be repeated in the same relation. (True or False)
<br />
<br />
<select id="item2" name="Item 2">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="True">True</OPTION>
    <OPTION VALUE="False">False</OPTION>
</select>
   <! -- Ansr2, False -->
<br />
<br />

   3. Which operator is used to compare a value to a specified list of values?
<br />
<br />

<select id="item3" name="Item 3">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="IN">IN</OPTION>
    <OPTION VALUE="ANY">ANY</OPTION>
    <OPTION VALUE="BETWEEN">BETWEEN</OPTION>
</select>
    <! -- Ansr3, IN -->
<br />
<br />

4. Which data manipulation command is used to combines the records from one or more tables?
<br />
<br />

<select id="item4" name="Item 4">
    <OPTION VALUE="null"> Select</OPTION>
    <OPTION VALUE="SELECT">SELECT</OPTION>
    <OPTION VALUE="JOIN">JOIN</OPTION>
    <OPTION VALUE="PRODUCT">PRODUCT</OPTION>
</select>
    <! -- Ansr4, JOIN -->
<br />
<br />
<br />
<button onclick="message()">Check your score here</button>
<br />

@using (Html.BeginForm("EditScore", "Tests", new { id = ViewData["id"] }, FormMethod.Post))
{
    <p>Please, enter your score in the below textbox after taking test!</p>
    <br />
    <input type="text" name="score" />
    <input type="Submit" />
    <br />
    <b>@Html.ActionLink("Submit", "SubmitSQL", new { })</b>}

<SCRIPT>
    function message() {
        count = 0
        var a1 = document.getElementById('item1');
        var a2 = document.getElementById('item2');
        var a3 = document.getElementById('item3');
        var a4 = document.getElementById('item4');
        if (a1.value == "null" || a2.value == "null" || a3.value == "null" || a4.value == "null") {
            alert('Please answer all the questions')
        }
        else {


            if (a1.value == "DECIMAL") {
                count = count + 1
            }
            if (a2.value == "False") {
                count = count + 1
            }
            if (a3.value == "IN") {
                count = count + 1
            }
            if (a4.value == "JOIN") {
                count = count + 1
            }
            alert('Your Score is ' + (count / 4 * 100) + '%')
        }
        var percentage = (count / 4) * 100;
        return percentage;
    }</SCRIPT>
